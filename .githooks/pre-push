#!/bin/sh
set -eu

repo_root="$(git rev-parse --show-toplevel)"
cd "$repo_root"

if ! command -v node >/dev/null 2>&1; then
  echo "[Crownlands] Node 22 is required. Install or activate it before pushing." >&2
  exit 1
fi

node tools/pre-push-check.js "$@"
