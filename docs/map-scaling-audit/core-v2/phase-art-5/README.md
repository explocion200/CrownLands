# Crownlands Core v2 Phase ART-5

Phase ART-5 creates exactly five development-only East / Southeast Core coordinates directly at the locked ART-2 v2 / ART-3 / ART-4 final-art standard. It does not change production runtime or world data, does not generate the final South row, and does not activate any candidate.

## Capacity correction

The first five approved maps represent 305 cities and the B1 / ART-3 maps represent 280. The first ten therefore represent 585, not 580. ART-4 adds 300, so the fifteen maps before ART-5 represent **885** cities, not 880. ART-5 adds 300 for **20 maps / 1,185 cities**. The remaining South batch is 295 and the final locked total remains **25 maps / 1,480 cities**. No city layout or capacity changed for this arithmetic correction.

## ART-5 maps

| Map | Coordinate | Capacity | Objective |
|---|---:|---:|---|
| East Deed Camp | `(2,-1)` | 60 | Deed Camp `(742,533)` |
| Swiftgate | `(1,0)` | 60 | Stronghold exact center `(724,543)` |
| East Support | `(2,0)` | 70 | none |
| SE Holding Tower | `(1,1)` | 55 | reservation `(736,555)`; no Tower art/mechanics |
| East / Southeast Relic Camp | `(2,1)` | 55 | Relic Camp `(746,558)` |

All geometry packages are deterministic, permanent-Core, spawn-ineligible, development-only definitions. Exactly four cardinal road sockets are retained. Eastward Core-boundary edges remain GATED where no Core neighbor exists; no diagonal topology is introduced.

Interactive Browser QA remains `DEFERRED_EXTERNAL_TOOL_BLOCK` because of `CODEX_BROWSER_LOCAL_ORIGIN_PERMISSION`. It is still production-blocking and not art-production-blocking. All 25 maps require a consolidated interactive runtime review before production use.
