# Literal edge-contact requirement

All four approval maps place their natural barrier directly on the image boundary. Barrier pixels are cropped by source pixel 0 on the top/left and source pixel 1447/1085 on the right/bottom. There is no exterior ground band, empty padding, haze frame, or floating inner ring.

Exactly one centered road breaks through each cardinal side. The road reaches the image boundary directly. No OPEN/GATED state is baked into the map; those remain future runtime overlays over the same road passage.

Each sample includes four full edge close-ups and a boundary-contact proof mosaic sampled directly from the normalized image's outermost pixels.
